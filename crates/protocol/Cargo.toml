[package]
name = "forge-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "TLS-mandatory network listener for ForgeDB"

[dependencies]
forge-types = { workspace = true }
